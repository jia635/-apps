<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout android:id="@+id/RelativeLayout_Item" android:paddingBottom="5.0dip" android:layout_width="fill_parent" android:layout_height="wrap_content"
  xmlns:android="http://schemas.android.com/apk/res/android">
    <ImageView android:id="@+id/item_image"
        android:layout_width="@dimen/menu_img_size"
        android:layout_height="@dimen/menu_img_size"
        android:layout_centerHorizontal="true"
        android:src="@mipmap/ic_setting"
        />
    <TextView
        android:id="@+id/item_text"
        android:textSize="@dimen/menu_text_size"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="设置"
        android:layout_below="@id/item_image"
        android:layout_centerHorizontal="true" />
</RelativeLayout>